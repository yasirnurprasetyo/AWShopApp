<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yasir.awshop">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AWShop"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activitys.PembayaranActivity"></activity>
        <activity android:name=".ui.activitys.ListAlamatActivity" />
        <activity android:name=".ui.activitys.TambahAlamatActivity" />
        <activity android:name=".ui.activitys.PengirimanActivity" />
        <activity android:name=".ui.activitys.DetailProdukActivity" />
        <activity android:name=".ui.activitys.RegisterActivity" />
        <activity android:name=".ui.activitys.MasukActivity" />
        <activity android:name=".ui.activitys.LoginActivity" />
        <activity android:name=".ui.activitys.BaruActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>